[package]
name = "cac"
version = "0.1.0"
edition = "2024"
description = "UBL 2.5 cac"

[dependencies]
serde = { version = "1", features = ["derive"] }
cct = { path = "../cct" }
ext = { path = "../ext" }
udt = { path = "../udt" }

[dev-dependencies]
serde_json = "1"
