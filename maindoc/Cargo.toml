[package]
name = "maindoc"
version = "0.1.0"
edition = "2024"
description = "UBL 2.5 document types — all business document definitions generated from the OASIS XSD schemas with xsd-parser"

[dependencies]
common = { path = "../common" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
